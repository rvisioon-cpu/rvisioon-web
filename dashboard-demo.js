(()=>{const reduced=matchMedia('(prefers-reduced-motion: reduce)');document.querySelectorAll('.real-ui').forEach(ui=>{let started=false;function run(){if(started)return;started=true;const numbers=ui.querySelectorAll('[data-count]');const start=performance.now();ui.classList.add('playing');function frame(now){const done=reduced.matches||document.body.classList.contains('paused');const p=done?1:Math.min(1,(now-start)/3800);numbers.forEach(n=>n.textContent=Math.round(Number(n.dataset.count)*(1-Math.pow(1-p,3))).toLocaleString('es-PE'));if(p<1)requestAnimationFrame(frame)}requestAnimationFrame(frame)}new IntersectionObserver(entries=>{if(entries.some(e=>e.isIntersecting))run()},{threshold:.65}).observe(ui)})})();
